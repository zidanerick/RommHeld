# Transfer scope

The generic Send File workflow accepts arbitrary local files and explicit destinations. The current Vita implementation adds same-size skipping, explicit overwrite confirmation, cancellation, and size verification. Future device backends reuse the same semantics.
